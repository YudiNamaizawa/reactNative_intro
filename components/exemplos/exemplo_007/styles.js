import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#FFF',
    }, 
    buttons: {
      flexDirection: 'row', 
      flexWrap: 'wrap'
    }
  });

  export default styles;